import React from 'react';

export const metadata = {
    title: 'About Page'
}

const AboutPage = () => {
    return (
        <div>
            this is about
        </div>
    );
};

export default AboutPage;